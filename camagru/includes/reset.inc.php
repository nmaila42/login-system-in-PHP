<?php

if (isset($_POST["Send Request"])){

    $selector = bin2hex(random_bytes(8));
    $token = random_bytes(32);

    url == "http://localhost/camagru/create_new_password.php";
    //create an expirey date for the token

} else {
    header ("Location: ../index.php");
}

?>